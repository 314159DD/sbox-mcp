# sbox-mcp reads no environment variables today.
# The MCP endpoint is fixed at 127.0.0.1:8080 (see plugin/src/Server/McpServerHost.cs).
# Tool descriptions and schemas live in plugin/config/tools.json.
